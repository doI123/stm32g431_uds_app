# ============================================================================
#  行尾/文本属性策略
#
#  为什么用 "* -text"（禁止一切自动转换）而不是常见的 "text=auto"：
#    1) 本工程的一致性校验依赖**逐字节一致**：
#         - tools/fill_fw_info.py 对 .bin 计算镜像 CRC32，上位机通过 DID 0xF184 读取；
#         - mcan.c 要求与 BOOT 侧 SHA256 可比对。
#       任何行尾转换都会让"改一行注释却导致哈希全变"式的问题出现。
#    2) 工程内现有文件**全部是 CRLF**，启用转换会产生全仓库 diff 噪音。
#
#  结果：仓库内按字节原样保存，克隆/检出后行尾与原作者一致。
# ============================================================================
* -text
